/*
 * JVM Bridge Server — Project Settings
 * =======================================
 * Configures the project name and includes the version catalog for dependency management.
 */

rootProject.name = "jvm-bridge-server"
