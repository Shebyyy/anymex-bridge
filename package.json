{
  "name": "anymex-bridge",
  "version": "0.1.0",
  "description": "AnymeX Extension Runtime Bridge — SSH server that runs the AnymeX JAR and routes per-user extension requests",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts"
  },
  "dependencies": {
    "ssh2": "^1.16.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.0.0"
  }
}
